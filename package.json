{
  "name": "aibrief-mcp",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Read-only remote MCP server (Cloudflare Worker) over aibrief's daily curated Korean AI briefings.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0",
    "agents": "^0.17.4",
    "zod": "^3.25.46"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260716.1",
    "typescript": "^5.6.0",
    "wrangler": "^4.111.0"
  }
}
